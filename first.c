#include<stdio.h>
int getsmax(int arr[], int n)
{
    int max =  arr[0];
    int smax= -1;
    forint i=1, i<n; i++)
    {
        if (arr[i] > max)
        {
            smax = max;
            max = arr[i]
        }
    }
}